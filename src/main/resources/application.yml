spring:
  application:
    name: campus-management

  datasource:
    url: jdbc:postgresql://localhost:5432/campus_management
    username: postgres
    password: Mugilan205!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080

app:
  cors:
    allowed-origins:
      - http://localhost:3000
      - http://localhost:5173
      - http://127.0.0.1:3000
      - http://127.0.0.1:5173
    allowed-methods:
      - GET
      - POST
      - PUT
      - PATCH
      - DELETE
      - OPTIONS
    allowed-headers:
      - "*"
    exposed-headers:
      - Authorization
    allow-credentials: true
    max-age: 3600

jwt:
  secret: 2d7eaf9b0f4d6c3a8e5b1f9c4783h332d3e5f7a9b2c4d6e8f1a3b5c7d9e
  expiration: 3600000

logging:
  level:
    org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping: TRACE