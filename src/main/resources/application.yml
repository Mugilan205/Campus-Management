spring:
  application:
    name: campus-management

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.mongodb.MongoAutoConfiguration
      - org.springframework.boot.autoconfigure.data.mongo.MongoDataAutoConfiguration

  datasource:
    url: jdbc:postgresql://localhost:5432/campus_management
    username: postgres
    password: Mugilan205!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080